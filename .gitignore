# Dependencies
node_modules/

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Environment — never commit real credentials
.env
.env.*
.env.local
!.env.example
*.key
*.pem
*.secret

# Build output
dist/
.next/

# Expo / React Native generated
app/ios/
app/android/
app/.expo/

# Generated pipeline outputs — run scripts to reproduce locally
unsiloed-test/parsed_output*.json
unsiloed-test/chunks.json

# PDFs — source documents stay local
*.pdf

# Vendored reference repo
moss-hacker-starter/
.moss/

# Editor / OS
.claude/
.DS_Store

# Root-level lockfiles (each subproject manages its own)
/package-lock.json
/package.json